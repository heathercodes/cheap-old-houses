# Cheap Old Houses: server-side

This app uses Node (Koa), Cheerio and Apollo Server with GraphQL

## Scripts
* run independently: `npm start`
* test suite: `npm test`
* run scraper: `cd scripts` `node scraper.js`
